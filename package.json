{
  "name": "regent",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/artomily/regent.git"
  },
  "bugs": {
    "url": "https://github.com/artomily/regent/issues"
  },
  "homepage": "https://github.com/artomily/regent#readme",
  "workspaces": [
    "frontend",
    "ai-agent"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "agent": "npm run dev --workspace ai-agent",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "contract:build": "forge build --root contract",
    "contract:test": "forge test --root contract"
  }
}
